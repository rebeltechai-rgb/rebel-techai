from ml_tools import preview_labels, preview_features, validate_dataset

print("\n=== LABELS PREVIEW ===")
print(preview_labels())

print("\n=== FEATURES PREVIEW ===")
print(preview_features())

print("\n=== VALIDATION ===")
print(validate_dataset())
